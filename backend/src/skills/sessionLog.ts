// OpenClaw Skill: Session Logger
// Called after every tutor session to persist session data to OpenClaw memory.
// Keeps a full history of what was covered, mistakes made, and progress.

export const sessionLogSkill = {
  name: 'session-log',
  description: 'Logs tutor session data to persistent OpenClaw memory',
  // TODO: implement skill logic
  // Input: studentId, subject, topic, duration, mistakes[], score
  // Action: append to student memory in OpenClaw
  // Output: updated memory confirmation
}
